# Copy to ".env" on the server and fill in a long random secret.
# Generate one with:  openssl rand -hex 32
# The SAME value must be set as ONLYOFFICE_JWT_SECRET in your Vercel project.
ONLYOFFICE_JWT_SECRET=replace_with_a_long_random_secret
